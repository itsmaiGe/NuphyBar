# NuphyBar 0.5.8

首个公开版本。让 NuPhy Air60 V2 的右侧灯条显示本机 AI Agent 状态。

## 下载

- `NuphyBar-0.5.8-macOS-arm64.dmg` — macOS 14+，Apple Silicon
- `NuphyBar-Air60-V2-stable-v7.bin` — **仅限 NuPhy Air60 V2 ANSI**
- `SHA256SUMS` — Release 文件校验值

## 主要功能

- 支持 Codex、Claude Code、OpenCode、Grok Build、Hermes 和 OpenClaw；
- 工作中蓝色波浪、等待/错误琥珀双脉冲、完成绿色呼吸；
- 空闲恢复原厂彩虹/电量，左侧 Caps Lock 保持原样；
- Mac 只在状态变化时发送一次 BLE HID 报告，动画在键盘本地运行；
- Air60 V2 固件基于 NuPhy 官方 v2.1.5 的 4 字节最小调用补丁，并提供可复现构建器和布局验证。

## 安装提示

DMG 使用 ad-hoc 签名，尚未经过 Apple Developer ID 公证。首次启动如被 macOS 拦截，请右键应用选择“打开”，并在系统设置中允许输入监控。

固件刷写前必须确认型号、备份 VIA、准备官方恢复文件并核对 SHA-256。Air60 V2 固件不能刷入其他 Air V2 尺寸。

---

First public release. NuphyBar shows local AI agent state on the right side light of a NuPhy Air60 V2.

The DMG targets macOS 14+ on Apple Silicon and is ad-hoc signed but not Apple-notarized. The firmware asset is **only for the NuPhy Air60 V2 ANSI**. Export VIA, keep the official recovery image, and verify checksums before flashing.
